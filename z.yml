plugins: ['core', 'svn', 'env', 'uglifyjs', 'bower']

vcs:
    url: svn://zon/projecten/zicht-libraries/ZichtImagePreloader.js

env:
    development:
        ssh:    ~
        web:    ~
        db:     ~
        url:    ~
        root:   ~

uglifyjs:
    config: ./uglifyjs.yml