plugins: ['core', 'zsass']

zsass:
    dirs: [ 'demo' ]

tasks:
    lint:
        - jshint ./src
        - jscs ./src -c ./src/.jscsrc
    update:
        - npm install
        - bower install
        - @zsass.update
