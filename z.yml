plugins: ['core', 'uglifyjs', 'zsass']

uglifyjs:
    config: ./uglifyjs.yml

zsass:
    dirs: [ 'demo' ]

tasks:
    setup:
        - npm install
        - bower install
        - @zsass.update
        - @uglifyjs.update